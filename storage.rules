rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow all authenticated users to upload and anyone to read
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
